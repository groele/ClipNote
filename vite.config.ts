// This file is intentionally minimal. Use the build script which runs
// vite.config.bg.ts (IIFE for background/content) then vite.config.sp.ts (ESM for sidepanel).
// See package.json "build" script.
export default {};
